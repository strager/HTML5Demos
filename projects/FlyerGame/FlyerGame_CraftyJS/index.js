window.onload = (function() {
	
	//1. LOADED BEFORE
	//The game is broken into multiple JS files
	//See other JS files loaded by './index.html' for more game logic
	
	//2. LOADED NOW
    // INITILIZE AND SHOW FIRST SCENE
    restartGame();
    
});
